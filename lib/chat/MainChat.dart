import 'package:flutter/material.dart';

class MainChat extends StatelessWidget {
  const MainChat({ Key key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('ini page chat'),
    );
  }
}