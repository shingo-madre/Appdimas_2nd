import 'package:flutter/material.dart';
import 'components/body.dart';

class VolunteerScreen extends StatelessWidget {
  static String routeName = "/volunteer";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
